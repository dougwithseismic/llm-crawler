import { createPuppeteerRouter } from 'crawlee'
import { defaultHandler } from './handlers/default-handler'
import { {{camelCase name}}PageHandler } from './handlers/{{kebabCase name}}-page-handler'

const create{{pascalCase name}}CrawleeRouter = () => {
    const router = createPuppeteerRouter()

    router.addDefaultHandler(defaultHandler) // Default handler for unspecified routes
    router.addHandler('{{constantCase name}}_PAGE', {{camelCase name}}PageHandler) // Specific handler for {{name}} pages

    return router
}

export const {{camelCase name}}CrawleeRouter = create{{pascalCase name}}CrawleeRouter()
